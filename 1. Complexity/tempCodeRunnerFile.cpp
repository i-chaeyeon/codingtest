
        // if (bin == N / bin && bin * bin == N) {
        //     return 1; // 제곱수임
        // } else if (bin > N / bin) {
        //     head = bin - 1;
        // } else {
        //     tail = bin + 1;
        // }